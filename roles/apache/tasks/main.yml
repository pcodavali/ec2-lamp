---
# tasks file for apache
- name: Ensure that httpd package is not installed on the system
  yum:
    name: httpd
    state: absent
- name: Ensure that httpd-tools package is not installed on the system
  yum:
    name: httpd-tools
    state: absent